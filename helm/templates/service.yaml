apiVersion: v1
kind: Service
metadata:
  name: {{ include "jira-outlook-addin.fullname" . }}
  labels:
    {{- include "jira-outlook-addin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jira-outlook-addin.selectorLabels" . | nindent 4 }}
